<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="IdeaMember">

	<select id="checkloInfo" resultType="int" parameterType="map">
		SELECT
		USER_ID FROM IDEA_MEMBER WHERE USER_ID=#{userId} and
		PASSWORD=#{password}
	</select>

	<insert id="createLogInfo" parameterType="IdeaMemberVO">
		INSERT INTO MEMBER
		(USER_ID, PASSWORD, AGE, NAME)
		VALUES
		(#{userId}, #{password}, #{age}, #{userName})
	</insert>

</mapper>