<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="rankboard">
	<select id="selectBoards" resultType="RankBoardVO">
		<!-- SELECT * FROM PJ_BOARD ORDER BY IDX DESC -->
<![CDATA[
		 SELECT a.BOARD_IDX, a.TITLE, a.REGDATE, a.CONTENTS, a.USER_ID, a.MOD_DATE, a.CONFIRM, a.HITCNT, a.FILE_ID        
		  FROM (
			SELECT ROWNUM rn, a.*
			  FROM (
				SELECT  *
				  FROM IDEA_BOARD ORDER BY BOARD_IDX DESC) a
			 WHERE ROWNUM <= 30 * 1) a
		 WHERE rn > 30 * (1 - 1)
		 ]]>
	</select>
</mapper>